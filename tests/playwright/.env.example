# Playwright E2E Test Environment Variables

# Base URLs
PLAYWRIGHT_BASE_URL=http://localhost:3001
API_BASE_URL=http://localhost:3000/api/v1

# Test User Credentials
# These should match a valid user in your test database
TEST_USER_EMAIL=test@devflow.local
TEST_USER_PASSWORD=TestPassword123!

# Database (for cleanup/setup)
DATABASE_URL=postgresql://devflow:changeme@localhost:5432/devflow?schema=public

# OAuth Encryption (required for integration tests)
OAUTH_ENCRYPTION_KEY=your-base64-encryption-key

# Test Project ID (optional - for specific project tests)
TEST_PROJECT_ID=your-test-project-id
